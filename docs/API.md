# API Reference

This project is a static frontend application and does not consume a backend API.

## Internal Data

- UI data comes from `src/constants/index.js`.
- No external network calls are performed by default.

